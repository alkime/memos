
# local dev testing of production build
services:
  app:
    container_name: memos-app
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - HUGO_BASEURL=http://localhost:8080/
    ports:
      - "8080:8080"
    environment:
      - ENV=production
      - GIN_MODE=release
    restart: unless-stopped
